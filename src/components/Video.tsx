
import { Video as VideoIcon } from 'lucide-react';

// Simple Video component that renders a video icon
const Video = ({ className }: { className?: string }) => {
  return <VideoIcon className={className} />;
};

export default Video;
